export enum FormType {
    AddRoute = 'addRoute',
    EditRoute = 'editRoute',
}

export enum InOut {
    In = 'in',
    Out = 'out',
}

export type ListItemWithId = {
    id: string;
};

export type Position = {
    x: number;
    y: number;
};
